# Concertable.Seed.Shared.UnitTests — unit tests

**Unit-only: a test that needs a host, HTTP, a container or a database belongs in a
`*.IntegrationTests` project, not here.**

Conventions: the `dotnet-standards:unit-testing` skill, plus `dotnet:unit-testing` for this system's
test-tier gate and the unit-tier bans.
